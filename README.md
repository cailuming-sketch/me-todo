# DOO 每日待办

## 本地运行
`npm install` 后运行 `npm run dev`，按终端地址打开网站。

## 验证
- `npm test`：运行全部自动化测试。
- `npm run build`：执行 TypeScript 检查并生成生产构建。

## 数据保存
任务保存在当前浏览器的 `localStorage` 键 `doo.todo.v1` 中。清除站点数据会删除任务；首版不支持账号或多设备同步。

## 首版范围
支持按日期管理任务、优先级、分类、备注、完成、删除撤销、顶部周历、完整月历和完成进度。
